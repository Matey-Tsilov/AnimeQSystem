@using AnimeQSystem.Web.Models.ViewModels.Anime

@model List<AnimeLongCardViewModel>;

<div id="animesContainer" class="container">
    <h1 class="text-center mb-4">Animes</h1>
    <div id="animeTable" class="d-flex flex-column gap-3">
        @{
            for (int i = 0; i < Model.Count(); i++)
            {
                <partial name="_AnimeLongCard" model="Model[i]"/>
            }
        }
    </div>
</div>